/**
 * ✔ type
 * ✔ size
 * ✔ color
 */
import * as React from 'react';
import { View, Image } from 'react-native';
import * as WEUI from '../../assets/weui';
const iconTypeMap = {
    success: 'SUCCESS',
    success_no_circle: 'SUCCESS_NO_CIRCLE',
    info: 'INFO',
    warn: 'WARN',
    waiting: 'WAITING',
    cancel: 'CANCEL',
    download: 'DOWNLOAD',
    search: 'SEARCH',
    clear: 'CLEAR',
};
const _Icon = ({ style, type, size = 23, color }) => {
    size = ~~size;
    const iconObj = WEUI[iconTypeMap[type]];
    if (!iconObj) {
        return (React.createElement(View, { style: [style, { width: size, height: size }] }));
    }
    const imageAsset = iconObj.image;
    // @todo Back & Delete do not have default color. '#B2B2B2'
    const iconColor = color || iconObj.defaultColor;
    return (React.createElement(View, { testID: 'icon', style: [style, { width: size, height: size }] },
        React.createElement(Image, { testID: 'image', source: imageAsset, style: {
                width: size,
                height: size,
                tintColor: iconColor
            } })));
};
_Icon.displayName = '_Icon';
export default _Icon;
//# sourceMappingURL=index.js.map