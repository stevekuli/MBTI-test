import * as React from 'react';
import { View } from 'react-native';
class _MovableArea extends React.Component {
    constructor(props) {
        super(props);
        this._onLayout = (event) => {
            const { width, height } = event.nativeEvent.layout;
            this.setState({
                width,
                height,
            });
        };
        this.state = {
            width: this.props.width || 100,
            height: this.props.height || 100,
        };
    }
    render() {
        const { style } = this.props;
        const { width, height } = this.state;
        return React.createElement(View, { style: [{ height, width, overflow: 'hidden' }, style], testID: 'moveableArea', onLayout: this._onLayout }, React.cloneElement(this.props.children, { layout: { width, height } }));
    }
}
_MovableArea.defaultProps = {};
export default _MovableArea;
//# sourceMappingURL=index.js.map