import { h, Host } from '@stencil/core';
import { notSupport } from '../../utils';
export class Map {
  componentDidLoad() {
    notSupport('Map', this);
  }
  render() {
    return (h(Host, null));
  }
  static get is() { return "taro-map-core"; }
}
